# cloudbuild.yaml (CORREGIDO)
# ... (líneas 1 a 10)
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'woo-mcp-mvp'
      - '--image'
      - 'gcr.io/$PROJECT_ID/woo-mcp-mvp'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      # 👇 DEBES AÑADIR ESTAS DOS LÍNEAS 👇
      - '--set-secrets'
      - 'CLIENTS=WOO_CLIENTS_CONFIG:latest' # Nombre de la variable = Nombre del Secreto : última versión
# ... (líneas 22 a 24)